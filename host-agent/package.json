{
  "name": "zeus-host-agent",
  "version": "1.0.0",
  "description": "Host Agent for Zeus IDE Integration — Express server running on host machine to manage IDE launches",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "@vitest/ui": "^1.0.4",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4",
    "tsx": "^4.7.0"
  }
}
